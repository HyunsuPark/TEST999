// Generated by CoffeeScript 1.10.0
var americano, host, port, start;

americano = require('americano');

port = process.env.PORT || 4000;

host = process.env.HOST || '0.0.0.0';

start = function(host, port, callback) {
  return americano.start({
    name: 'bookmark-couchdb',
    port: port,
    root: __dirname,
    host: host
  });
};

if (!module.parent) {
  port = process.env.PORT || 4000;
  host = process.env.HOST || '0.0.0.0';
  start(host, port, function(err) {
    if (err) {
      console.log("Initialization failed, not starting");
      console.log(err.stack);
      return process.exit(1);
    } else {
      return module.exports = start;
    }
  });
}
