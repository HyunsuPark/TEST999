#container
  label.
    URL
  input(type='text' class='form-control' ng-model='bookmarkURL')
  label.
    이름
  input(type='text' class='form-control' ng-model='bookmarkName')
  button(class='btn btn-primary' ng-click='addBookmark()').
    북마크 추가
  
  ul
    li(ng-repeat='bookmark in bookmarks')
      |<a href="{{bookmark.url}}" target="_blank">{{bookmark.title}}<a>
      button(ng-click='removeBookmark(bookmark)').
        삭제